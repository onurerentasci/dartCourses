/* Örnek 1: İki notu girilen öğrencinin ortalamasını bularak sonucu ekranda gösteren algoritmayı yapın.

Örnek 2: Fiyatı girilen ürüne %18 kdv ekleyerek son fiyatı hesaplayan algoritmayı yazın. */

import 'dart:io';

void main(List<String> args) {
  // cevap1

  /* print("İlk öğrencinin notunu giriniz : ");
  int? not1 = int.parse(stdin.readLineSync()!);
  print("İkinci öğrencinin notunu giriniz : ");
  int? not2 = int.parse(stdin.readLineSync()!);
  double ortalama = (not1 + not2) / 2;
  print("İki öğrencinin ortalaması = $ortalama"); */

  // cevap 2

  /* print("Almak istediğiniz ürünün fiyatını yazınız");
  int? fiyat = int.parse(stdin.readLineSync()!);
  double kdv = fiyat + ((fiyat * 18) / 100);
  print("Aldığınız ürünün vergisiz fiyatı = $fiyat, kdv sonrası fiyatı = $kdv"); */
  
}
